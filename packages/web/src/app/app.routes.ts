import { Routes } from "@angular/router";
import { GraphExplorerComponent } from "./features/graph-explorer/graph-explorer.component";

export const routes: Routes = [{ path: "", component: GraphExplorerComponent }];
